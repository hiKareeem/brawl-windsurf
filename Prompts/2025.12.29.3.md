---
Date: 2025-12-29
Intent: Next delegation prompts (post 2025.12.29.2 implementation)
Primary reference: rules/brawl-plan.md
---

## PROMPT 1 / Workstream: Scouting RPC hardening — reject requests from spectator/eliminated requesters + add automation

ROLE: Senior Unreal C++ engineer. You own hardening the **requester-side** validation for scouting.

CONTEXT:
- `ServerRequestScoutPlayer(TargetPlayerId)` currently validates the **target** (not spectator, not eliminated, has board) and is rate-limited.
- Project rule: spectators should not send gameplay requests (see validation checklist).

SCOPE (DO):
- Update `ABrawlPlayerController::ServerRequestScoutPlayer_Implementation` to reject requests when the **requesting** player is invalid:
  - If `OwnerPS->IsSpectator()` → reject (log if enabled) and do not change scouted state.
  - If `OwnerPS->GetLife() <= 0` → reject (log if enabled) and do not change scouted state.
- Add deterministic automation coverage in LVL_Sandbox validating:
  - A spectator requester cannot change `ScoutedPlayerId/ScoutedBoardActor`.
  - An eliminated requester cannot change `ScoutedPlayerId/ScoutedBoardActor`.

SCOPE (DO NOT):
- Do not add replicated fields.
- Do not add GameplayTags.
- Do not add UI.

DELIVERABLES:
- Code change in `Source/BrawlMatch/Private/Game/BrawlPlayerController.cpp`.
- Extend `Source/BrawlMatch/Private/Tests/BrawlScoutingAutomationTest.cpp` (or add a new test) with a deterministic scenario.

ACCEPTANCE CRITERIA:
- Rejections are logged via existing `LogRejectedRequest(...)` when `brawl.Net.LogRejectedRequests != 0`.
- Existing scouting happy-path tests still pass.

OUTPUT FORMAT:
- Files modified (paths)
- Test names
- Summary of assertions


## PROMPT 2 / Workstream: Scouting lifecycle — make `SetScoutedBoardActor` idempotent (avoid redundant teleports/camera triggers)

ROLE: Senior Unreal C++ engineer. You own removing redundant state churn in scouting state updates.

CONTEXT:
- `ABrawlPlayerState::SetScoutedBoardActor(InPlayerId, InBoardActor)` currently broadcasts and triggers a server teleport every time.
- Auto-follow updates can re-set the same `(PlayerId, BoardActor)` pair repeatedly in edge cases.

SCOPE (DO):
- Make `SetScoutedBoardActor(...)` a no-op if both:
  - `ScoutedPlayerId == InPlayerId` AND
  - `ScoutedBoardActor == InBoardActor`
- Keep the behavior unchanged when either value actually changes.
- Add/extend automation coverage to assert:
  - Repeated calls with identical values do not change state and do not spam delegates.
    - You may count delegate invocations by binding a counter to `OnScoutedBoardActorChanged()` in the test.

SCOPE (DO NOT):
- Do not change replication shape.
- Do not add new delegates/events.

DELIVERABLES:
- Code change in `Source/BrawlMatch/Private/Game/BrawlPlayerState.cpp`.
- Automation update in `Source/BrawlMatch/Private/Tests/BrawlScoutingAutomationTest.cpp`.

ACCEPTANCE CRITERIA:
- No behavior regression for normal scouting.
- Reduced redundant broadcasts/teleports in identical-set scenarios.

OUTPUT FORMAT:
- Files modified (paths)
- Test names
- Summary of behavior


## PROMPT 3 / Workstream: Scouting invalidation — reduce duplication by centralizing "clear viewers scouting X" logic

ROLE: Senior Unreal C++ engineer. You own small refactors that improve maintainability without changing behavior.

CONTEXT:
- The helper that clears viewers scouting a target player is currently duplicated in multiple translation units.
- It is used for:
  - `SetIsSpectator(true)` invalidation
  - `RemovePlayerState(...)` invalidation

SCOPE (DO):
- Centralize the behavior in **one** authoritative place (recommended: `ABrawlGameState`):
  - Add a server-only method like `ABrawlGameState::ServerClearScoutingForViewersOfPlayerId(int32 TargetPlayerId)`.
  - Call it from:
    - `ABrawlPlayerState::SetIsSpectator(true)`
    - `ABrawlGameState::RemovePlayerState(...)`
- Keep iteration deterministic (iterate `PlayerArray`, compare `PlayerId`).

SCOPE (DO NOT):
- No new modules.
- No new replicated fields.
- No new GameplayTags.

DELIVERABLES:
- Code changes in `Source/BrawlMatch/Private/Game/BrawlGameState.cpp` (+ header as needed) and `Source/BrawlMatch/Private/Game/BrawlPlayerState.cpp`.
- No test changes required if behavior is preserved, but acceptable if needed.

ACCEPTANCE CRITERIA:
- No behavior change (existing scouting tests still pass).
- Only one implementation of the "clear viewers scouting X" loop remains.

OUTPUT FORMAT:
- Files modified (paths)
- Summary of refactor
