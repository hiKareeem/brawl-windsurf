---
Date: 2025-12-29
Intent: Next delegation prompts (post-audit)
Primary reference: rules/brawl-plan.md
---

## PROMPT 1 / Workstream: Scouting (server-authored) — automation coverage + invariants

ROLE: Senior Unreal C++ engineer. You own adding **automation coverage** for the scouting pipeline.

CONTEXT:
- Scouting request flow exists (client request -> server validates -> server sets owner-only scouted state).
- Scouted board relevance is used by RepGraph; we want confidence it stays correct as we iterate on v3.

SCOPE (DO):
- Add a LVL_Sandbox latent automation test that validates, server-side:
  - `ABrawlPlayerController::ServerRequestScoutPlayer(TargetPlayerId)` sets `ABrawlPlayerState::ScoutedPlayerId` and `ScoutedBoardActor`.
  - Self-scout clears scouting state.
  - When a **scouted target** changes `ActiveBoardActor`, the viewer’s `ScoutedBoardActor` updates automatically to the new board (if implemented).

SCOPE (DO NOT):
- Do not change replication shape.
- Do not add new GameplayTags or Attributes.
- Do not touch UI assets.

IMPLEMENTATION GUIDELINES:
1) Locate the existing scouting code paths in:
   - `Source/BrawlMatch/Public+Private/Game/BrawlPlayerController.*`
   - `Source/BrawlMatch/Public+Private/Game/BrawlPlayerState.*`
2) Author the test using the project’s automation patterns:
   - open `LVL_Sandbox`
   - spawn `ABrawlPlayerController` + `InitPlayerState()` (do not use `UGameplayStatics::CreatePlayer`)
   - spawn `ABrawlBoardActor`s and assign via `PS->SetBoardActor(...)`
3) Keep it deterministic: fixed `MatchId`, stable `PlayerId`s.

DELIVERABLES:
- New test file under `Source/BrawlMatch/Private/Tests/`.
- Test name(s): propose something like:
  - `Brawl.Match.Scouting.ServerAuth.StateAndFollowActiveBoard.LVL_Sandbox`

ACCEPTANCE CRITERIA:
- Test is deterministic and passes in Editor automation suite.
- No new content assets.

OUTPUT FORMAT:
- Files modified/added (paths)
- Test names
- Brief summary of what is asserted


## PROMPT 2 / Workstream: ReplicationGraph — route player avatars/pawns with boards (spectator correctness)

ROLE: Senior Unreal C++ engineer. You own improving ReplicationGraph routing so **player avatars/pawns** are visible to viewers of a board, especially spectators.

CONTEXT:
- `UBrawlReplicationGraph` already routes boards/units/projectiles via per-board actor lists.
- Player avatars/pawns are currently expected to be cosmetic but replicated.
- The camera contract expects spectators who “see all boards” to also see all avatars.

SCOPE (DO):
- Audit current pawn/avatar replication behavior under RepGraph.
- If pawns are not board-scoped, implement a minimal routing strategy so that:
  - spectators who gather all board nodes also receive player pawns/avatars
  - non-spectators viewing a board reliably receive pawns on that board even if boards are far apart

SCOPE (DO NOT):
- Do not change gameplay authority.
- Avoid adding new replicated fields.
- Avoid large architecture changes; prefer a targeted RepGraph-side fix.

IMPLEMENTATION GUIDELINES (SUGGESTED APPROACH):
- In `UBrawlReplicationGraph::RefreshBoardScopedRouting()`:
  - iterate `APlayerController` or `APawn` actors
  - decide which `ABrawlBoardActor` they should belong to (e.g., via owning `ABrawlPlayerState::GetActiveBoardActor()` or nearest board by distance)
  - assign them to that board node using the existing `AssignActorToBoard` helper

DELIVERABLES:
- Code change in BrawlNet RepGraph that routes pawns/avatars appropriately.

ACCEPTANCE CRITERIA:
- Spectator connections that see all boards also see all pawns/avatars.
- No new modules/tags/attributes.

OUTPUT FORMAT:
- Files modified (paths)
- Summary of routing rule used (ActiveBoard vs nearest board)
- Any known tradeoffs


## PROMPT 3 / Workstream: Scouting RPC hardening — consistent rate limiting + rejection logging

ROLE: Senior Unreal C++ engineer. You own hardening the **scouting request RPC** so it matches the project’s server-validation posture.

CONTEXT:
- `ServerRequestScoutPlayer` exists and currently enforces a basic rate limit.
- Other RPCs follow a pattern of configurable CVars + optional rejection logging.

SCOPE (DO):
- Make scouting rate limiting consistent with other RPC patterns:
  - allow configuring the interval (CVar, or reuse an existing pattern)
  - optionally emit rejection logs when enabled
- Ensure validation is explicit and auditable:
  - invalid target player id => reject
  - target has no board/active board => reject
  - self-scout => clear scouting state

SCOPE (DO NOT):
- Do not add new event-log schema/events unless you STOP and ask.
- Do not change replication shape.

DELIVERABLES:
- Updated scouting RPC code.

ACCEPTANCE CRITERIA:
- Clear, consistent behavior and debugging signals.

OUTPUT FORMAT:
- Files modified (paths)
- Summary of validation + new configuration knobs
