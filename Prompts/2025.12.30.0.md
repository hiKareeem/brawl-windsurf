# 2025.12.30.0 — Brawl Delegation Prompts (in-IDE)

```yaml
meta:
  prompt_set: brawl_delegation_prompts
  version: 1
  date: 2025-12-30
  repo_roots:
    brawl_game: E:/Unreal/BrawlFinal/Brawl
  formatting_notes:
    - "Each prompt is intended to be pasted as-is into an assistant session."
    - "Assume UE 5.7, dedicated server authority, ReplicationGraph enabled."
```

---

```yaml
prompt_id: brawl_ws1_scouting_camera_ux
title: "Complete scouting + board-switch camera UX (client presentation only)"
priority: high
status_context:
  brawl_plan_focus: "v3.3 Spectator/scouting UX (camera polish pending)"
  recent_code_changes:
    - "ServerRequestScoutPlayer now validates requester/target + rate limits"
    - "ActiveBoardActor / ScoutedBoardActor changes now trigger camera hooks"

goal:
  - "Make camera behavior feel deterministic and intuitive when:"
  - "1) switching ActiveBoardActor (arena transfer/return)"
  - "2) starting/stopping scouting (ScoutedBoardActor changes)"
  - "3) scouting becomes invalid (target eliminated/spectator/no board)"

constraints:
  authority_and_rules:
    - "Do NOT move any gameplay authority into UI/camera code. Camera is client-only presentation."
    - "Do NOT add new GameplayTags, Attributes, or DataAsset types without stopping for approval."
    - "Do NOT change replication shape/policies; work within existing replicated fields + delegates."
  architecture:
    - "BrawlUI must remain read-only (no gameplay rules)."
    - "Prefer idempotent state transitions; avoid per-tick polling."

key_apis_and_signals_to_use:
  - "ABrawlPlayerState::OnActiveBoardActorChanged (delegate)"
  - "ABrawlPlayerState::OnScoutedBoardActorChanged (delegate)"
  - "ABrawlPlayerController::HandleActiveBoardActorChanged(...)"
  - "ABrawlPlayerController::HandleScoutedBoardActorChanged(...)"
  - "ABrawlPlayerController::ForceBoardCameraMode()"
  - "ABrawlPlayerController::ApplyAutoFollowActiveBoardCameraView()"
  - "ABrawlPlayerController::ApplyScoutingCameraView()"

files_to_audit_and_edit:
  brawl_match:
    - "Source/BrawlMatch/Public/Game/BrawlPlayerController.h"
    - "Source/BrawlMatch/Private/Game/BrawlPlayerController.cpp"
    - "Source/BrawlMatch/Public/Game/BrawlPlayerState.h"
    - "Source/BrawlMatch/Private/Game/BrawlPlayerState.cpp"
  brawl_ui (read-only; only if a missing binding is discovered):
    - "Source/BrawlUI/Public/UI/BrawlVM_Scouting.h"
    - "Source/BrawlUI/Private/UI/BrawlVM_Scouting.cpp"
  blueprint_surface_area:
    - "Any BP implementation that responds to ForceBoardCameraMode / scouting camera view"

work_items:
  - id: ws1_1
    description: "Build a truth table for camera mode selection based on (IsScouting?, ActiveBoardActor, ScoutedBoardActor)."
  - id: ws1_2
    description: "Ensure scouting overrides auto-follow: when ScoutedBoardActor is set, apply scouting camera view; when cleared, restore auto-follow."
  - id: ws1_3
    description: "Handle invalidation: when scouting is cleared indirectly (e.g., target eliminated -> GameState clears viewers), ensure camera returns to a valid board and does not get stuck."
  - id: ws1_4
    description: "Audit idempotency: repeated ScoutedBoardActor set to same (PlayerId, Board) should not re-run expensive transitions."
  - id: ws1_5
    description: "Confirm teardown safety: any OnRep/delegate handlers should early-out when World is null/tearing down."

acceptance_criteria:
  - "Switching scouting on/off results in exactly one coherent camera transition each time (no flicker/loops)."
  - "Arena transfer ActiveBoardActor changes keep camera correct (auto-follow when not scouting; scouting view stays stable when scouting)."
  - "No gameplay logic added; no replication changes."

tests:
  - "Do not add camera-only automation assertions unless you can do so without local-player APIs (dedicated server constraints)."
  - "At minimum, ensure existing scouting automation tests still pass (BrawlScoutingAutomationTest)."

rules_applied:
  - "brawl-implementation.md: Dedicated server authoritative only; camera is client-only."
  - "brawl-replication-contract-2.md: ReplicationGraph board routing + scouting policy."
  - "brawl-style-and-workflow.md: BrawlUI read-only; avoid tick; teardown-safe handlers."

deliverables:
  - "Patch (C++): deterministic camera mode selection + edge-case handling"
  - "(Optional) BP notes: what to wire in ForceBoardCameraMode / camera rigs to reflect new semantics"
```

---

```yaml
prompt_id: brawl_ws2_scoreboard_elimination_polish
title: "Scoreboard + elimination polish (replication + UI bindings hardening)"
priority: high
status_context:
  recent_code_changes:
    - "Life <= 0 triggers SetIsSpectator(true)"
    - "Scouting cleanup centralized in ABrawlGameState::ServerClearScoutingForViewersOfPlayerId"
    - "Scouting RPC rejects requester/target eliminated or spectator"

goal:
  - "Ensure scoreboard-facing fields update reliably for all viewers (players + spectators)"
  - "Ensure elimination -> spectator transition is robust and cleans up any dependent state (notably scouting)"

constraints:
  - "Do NOT change visibility policy unless explicitly requested; assume v3 visibility rules."
  - "Do NOT add new replicated fields unless you stop and ask first (replication shape change)."
  - "Do NOT introduce new gameplay tags/attributes."

files_to_audit_and_edit:
  brawl_match:
    - "Source/BrawlMatch/Public/Game/BrawlPlayerState.h"
    - "Source/BrawlMatch/Private/Game/BrawlPlayerState.cpp"
    - "Source/BrawlMatch/Public/Game/BrawlGameState.h"
    - "Source/BrawlMatch/Private/Game/BrawlGameState.cpp"
  brawl_ui:
    - "Source/BrawlUI/Public/UI/BrawlVM_Scouting.h"
    - "Source/BrawlUI/Private/UI/BrawlVM_Scouting.cpp"

work_items:
  - id: ws2_1
    description: "Audit which fields the scouting scoreboard UI reads (Life, Gold, XP/Level, streaks, spectator flag) and verify refresh triggers exist (delegates/OnRep paths)."
  - id: ws2_2
    description: "Ensure ABrawlPlayerState broadcasts ScoreboardFieldsChanged for all scoreboard-relevant mutations (life delta, win/loss record, last opponent updates, economy/progression changes if UI depends on them)."
  - id: ws2_3
    description: "Confirm elimination path is single-shot + consistent: OldLife>0 && Life<=0 => SetIsSpectator(true), and GameState clears scouting for viewers of this player."
  - id: ws2_4
    description: "UI hardening: ensure eliminated/spectator players are displayed correctly and not offered as valid scout targets (presentation only; server remains authoritative)."
  - id: ws2_5
    description: "Add/extend automation coverage ONLY where server-side observable (delegate counts, replicated state)."

acceptance_criteria:
  - "Scoreboard UI updates when life/gold/xp/level/streak change (no stale entries)."
  - "When a player is eliminated, any viewers scouting them are cleared promptly and end up in a valid state."
  - "No replication policy changes; no new tags/attributes."

tests:
  - "Extend Source/BrawlMatch/Private/Tests/BrawlScoutingAutomationTest.cpp if needed:"
  - "- verify clearing scouting when target becomes eliminated"
  - "- verify requester/target spectator rejections remain correct"

rules_applied:
  - "brawl-replication-contract-2.md: Visibility policy (v3) + scouting replication routing."
  - "brawl-server-validation-checklist.md: scouting request validation + spectator rules."
  - "brawl-testing-matrix.md: automation constraints (dedicated server compatible)."

deliverables:
  - "Patch (C++/MVVM): scoreboard refresh correctness + elimination cleanup robustness"
  - "Automation test updates (server-observable only)"
```

---

```yaml
prompt_id: brawl_ws3_multi_arena_orchestration_hardening
title: "Multi-arena combat orchestration hardening (transfer/return + ghost roster edge cases)"
priority: medium
status_context:
  brawl_plan_focus: "v3.1 Multi-arena orchestration implemented; harden edge cases"
  existing_automation:
    - "Brawl.Match.MultiArena.Orchestration.4P_2Arenas.LVL_Sandbox"
    - "Brawl.Match.GhostRoster.ArenaResolvedEvent.LVL_Sandbox"

goal:
  - "Eliminate remaining edge-case failures in arena transfer/return across N concurrent arenas"
  - "Harden ghost roster snapshot lifecycle and cleanup"
  - "Preserve determinism: stable ordering by UnitInstanceId where relevant"

constraints:
  - "Do NOT change core match flow semantics (Planning->Combat->Rewards) without approval."
  - "Do NOT add new replicated fields or change replication graph routing without approval."
  - "Do NOT add per-unit ticking; keep logic centralized (GameState/RoundManager/CombatManager)."

files_to_audit_and_edit:
  brawl_match:
    - "Source/BrawlMatch/Public/Game/BrawlGameState.h"
    - "Source/BrawlMatch/Private/Game/BrawlGameState.cpp"
    - "Source/BrawlMatch/Private/Components/BrawlRoundManagerComponent.cpp"
  tests:
    - "Source/BrawlMatch/Private/Tests/BrawlMultiArenaOrchestrationAutomationTest.cpp"
    - "Source/BrawlMatch/Private/Tests/BrawlGhostRoster_ArenaResolvedEventAutomationTest.cpp"

work_items:
  - id: ws3_1
    description: "Audit transfer/return invariants: no duplicate authoritative occupancy; return restores correct home coords; bench shuffles during combat persist on return." 
  - id: ws3_2
    description: "Validate deterministic iteration order everywhere a set of units is processed (sort by FBrawlUnitInstanceId.Value)."
  - id: ws3_3
    description: "Ghost roster lifecycle: ensure spawn uses server authority and cleanup is guaranteed on phase transitions + match end; no leaked actors/occupancy." 
  - id: ws3_4
    description: "Add focused automation cases for any bug found (must be dedicated-server safe; no CreatePlayer)."

acceptance_criteria:
  - "Existing multi-arena + ghost roster automation tests pass consistently."
  - "No orphaned units remain on inactive boards after return/cleanup (authoritative occupancy matches expected)."
  - "No replication shape changes; no new tags/attributes."

rules_applied:
  - "brawl-implementation-2.md: arena transfer mapping + return semantics + stable ordering."
  - "brawl-replication-contract-2.md: board occupancy is canonical; transforms derived."
  - "brawl-performance-budgets.md: avoid per-unit tick; centralized updates; RepGraph usage."

deliverables:
  - "Patch (C++): transfer/return/ghost hardening"
  - "Automation test additions for any fixed edge cases"
```
