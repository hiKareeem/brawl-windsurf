```yaml
meta:
  prompt_set: brawl_delegation_prompts
  version: 1
  date: 2025-12-30
  repo_roots:
    brawl_game: E:/Unreal/BrawlFinal/Brawl
  formatting_notes:
    - "Each prompt is intended to be pasted as-is into an assistant session."
    - "Assume UE 5.7, dedicated server authority, ReplicationGraph enabled."
    - "You may build/compile/test but do not run/package."
```

---

```yaml
prompt_id: brawl_ws4_camera_rig_gameplaycameras_polish
title: "GameplayCameras + GASP camera rig polish for Board/Scouting modes (content + client-only presentation)"
priority: high
status_context:
  brawl_plan_focus: "v3.3 Spectator/scouting UX (camera rig/UX polish pending)"
  implemented_hooks:
    - "ABrawlPlayerController::ApplyDesiredBoardCameraView() state machine exists"
    - "ABrawlPlayerController::ForceBoardCameraMode() exists (BP surface area)"
    - "ABrawlPlayerState::SetActiveBoardActor / SetScoutedBoardActor trigger server teleports"

goal:
  - "Create/finish the actual camera rigs and BP glue so Board Camera and Scouting feel TFT-like:"
  - "1) smooth blends on ActiveBoard changes (arena transfer/return)"
  - "2) smooth blends when starting/stopping scouting"
  - "3) no camera stuck state when scouted board is temporarily invalid during transfer (fallback to auto-follow)"

constraints:
  authority_and_rules:
    - "Camera is client-only presentation; do not move gameplay authority into camera/BP."
    - "Do NOT add new replicated fields, change replication conditions, or change RepGraph routing without approval."
    - "Do NOT add new GameplayTags, Attributes, or DataAsset types without stopping for approval."
  implementation:
    - "Prefer Blueprint / GameplayCameras assets for rigs; keep C++ changes minimal and only for plumbing."
    - "Do not introduce per-tick polling for camera mode; use the existing delegate-driven hooks."

files_to_audit_and_edit:
  brawl_match:
    - "Source/BrawlMatch/Public/Game/BrawlPlayerController.h"
    - "Source/BrawlMatch/Private/Game/BrawlPlayerController.cpp"
  content:
    - "Content/Brawl/Blueprints/... (player avatar / camera)"
    - "Content/Brawl/Blueprints/... (GameplayCameras assets, if any)"

work_items:
  - id: ws4_1
    description: "Inventory existing camera-related BPs/assets and confirm how ForceBoardCameraMode is implemented/wired."
  - id: ws4_2
    description: "Implement/finish a Board camera rig (top-down / board-framing) and a Scouting framing behavior anchored on Board->TryGetViewTargetTransformForPlayer(TargetPlayerId)."
  - id: ws4_3
    description: "Implement blend policy knobs (blend time, easing, camera offsets) as BP-exposed tuning (no hardcoded constants)."
  - id: ws4_4
    description: "Validate spectator flow: ApplyBoardCameraViewToSpectatorIfNeeded() path should work for spectators and players without special casing."

acceptance_criteria:
  - "Local camera transitions occur exactly once per state change (ActiveBoardActor or ScoutedBoardActor)."
  - "Scouting overrides auto-follow; stopping scouting returns to auto-follow ActiveBoardActor."
  - "No gameplay side effects; server authority unchanged."

rules_applied:
  - "brawl-player-avatar-camera-contract.md: camera is client-only; server-auth teleports"
  - "brawl-implementation.md: server authoritative gameplay"

deliverables:
  - "List BP assets to be updated/created for GameplayCameras rigs (names + paths)."
  - "Any minimal C++ plumbing patches if required (with rationale)."
```

---

```yaml
prompt_id: brawl_ws5_spectator_board_switching_ui_polish
title: "Spectator board switching + scouting UI polish (MVVM + widget bindings)"
priority: high
dependencies:
  - "Recommended: brawl_ws4_camera_rig_gameplaycameras_polish (so switching has a stable camera rig to drive)"

status_context:
  existing:
    - "UBrawlVM_Scouting exists and enumerates players/boards/units; it can issue scouting requests"
    - "ABrawlPlayerState broadcasts ScoreboardFieldsChanged; roster/economy/progression push updates exist"

goal:
  - "Make scouting/board switching UX feel complete and robust for players + spectators:"
  - "1) show reliable scoreboard list (Life/Gold/XP/Level/Streak/Spectator/Eliminated/Roster/Bench)"
  - "2) allow selecting a player to scout; disable invalid targets locally (presentation-only)"
  - "3) show current scouted target and keep it updated as the target moves arenas"

constraints:
  architecture:
    - "BrawlUI must remain read-only: no gameplay rules/validation in UI; server remains authoritative."
    - "Do NOT add new GameplayTags/Attributes/DataAsset types without stopping for approval."
    - "Do NOT change replication shape/policies without stopping for approval."

files_to_audit_and_edit:
  brawl_ui:
    - "Source/BrawlUI/Public/UI/BrawlVM_Scouting.h"
    - "Source/BrawlUI/Private/UI/BrawlVM_Scouting.cpp"
    - "Source/BrawlUI/Public/UI/BrawlUserWidgetBase.h"
    - "Source/BrawlUI/Private/UI/BrawlUserWidgetBase.cpp"
  content:
    - "Content widgets binding to UBrawlVM_Scouting (WBP_...)"

work_items:
  - id: ws5_1
    description: "Audit VM refresh triggers and ensure no stale entries for life/gold/xp/level/streak/spectator (push-based delegates should cover this)."
  - id: ws5_2
    description: "Add explicit VM fields per player entry: CanScout, bIsEliminated, bIsSpectator (derived from replicated PS fields)."
  - id: ws5_3
    description: "Handle edge cases in VM: ScoutedBoardActor temporarily invalid during arena transfer (display fallback state; avoid stuck selection)."
  - id: ws5_4
    description: "Only if needed: add small automation tests for server-observable scouting state changes (no client-only assertions)."

acceptance_criteria:
  - "UI never presents eliminated/spectator players as valid scout targets."
  - "Selecting a target triggers one server request and updates view promptly."
  - "Scouting follows target's ActiveBoardActor and the UI updates accordingly."

rules_applied:
  - "brawl-style-and-workflow.md: BrawlUI read-only; MVVM pattern"
  - "brawl-server-validation-checklist.md: UI presentation only; server validates and commits"

deliverables:
  - "Patch (C++ MVVM): scouting/scoreboard binding hardening"
  - "Optional: minimal widget binding notes or small BP widget edits if required"
```

---

```yaml
prompt_id: brawl_ws6_automation_endmatch_cleanup_regressions
title: "Add automation coverage for EndMatch cleanup (arena transfer + ghost roster) and teardown safety"
priority: medium

status_context:
  recent_code_changes:
    - "ABrawlGameState::EndMatch() performs best-effort cleanup (ghost snapshot + return transfer plan + restore ActiveBoardActor) before setting bMatchEnded"

goal:
  - "Prevent regressions where matches end mid-combat leaving players stuck on an arena board, leaving GuestPlayerId assigned, or leaking ghost units/occupancy."

constraints:
  testing:
    - "Dedicated-server safe (no local-player camera assertions; no CreatePlayer)."
    - "Prefer LVL_Sandbox latent tests; use the same harness style as existing BrawlMatch automation tests."
  scope:
    - "Focus on tests first. Only apply minimal fixes if a deterministic bug is found."
    - "Do NOT change core match flow semantics (Planning->Combat->Rewards) without approval."

files_to_audit_and_edit:
  tests:
    - "Source/BrawlMatch/Private/Tests/..."
  brawl_match:
    - "Source/BrawlMatch/Public/Game/BrawlGameState.h"
    - "Source/BrawlMatch/Private/Game/BrawlGameState.cpp"

work_items:
  - id: ws6_1
    description: "Write a latent test that sets up 2–4 players, forces an arena transfer plan active, optionally spawns a ghost roster snapshot, then calls GameState->EndMatch() and asserts cleanup invariants."
  - id: ws6_2
    description: "Assert invariants: for all players ActiveBoardActor == BoardActor; arena boards have GuestPlayerId cleared; ghost units are destroyed/unregistered; authoritative occupancy matches expected (no duplicates)."
  - id: ws6_3
    description: "If test uncovers a bug: apply minimal fix with deterministic ordering (sort by UnitInstanceId.Value) + teardown guards (World null/tearing down)."

acceptance_criteria:
  - "New test(s) pass reliably and enforce the EndMatch cleanup contract."

rules_applied:
  - "brawl-implementation.md: match lifecycle + server authority"
  - "brawl-testing-matrix.md: automation constraints"
  - "brawl-performance-budgets.md: avoid per-unit tick"

deliverables:
  - "New automation test(s) + minimal fixes if required"
```
