---
Date: 2025-12-29
Intent: Delegation prompts (v3 focus)
Primary reference: rules/brawl-plan.md
---

## PROMPT 1 / Workstream: v3.1 Multi-arena orchestration — automation stabilization

ROLE: Senior Unreal C++ engineer. You own debugging + fixing a failing/flaky automation test(s) around **multi-arena orchestration**.

CONTEXT:
- Per `rules/brawl-plan.md`, v3.1 multi-arena is implemented but **automation stabilization is in progress**.
- You must operate inside the Brawl repo only: `E:/Unreal/BrawlFinal/Brawl/`.

SCOPE (DO):
- Find the automation test(s) that cover multi-arena transfer/return / multi-pair combat start/end.
- Make the test(s) deterministic and/or fix the underlying runtime code if the behavior is wrong.
- Add minimal logging or test helper usage if needed to root-cause determinism issues.

SCOPE (DO NOT):
- Do not implement v4+ items.
- Do not introduce new modules.
- Do not introduce new GameplayTag namespaces or new Attributes.
- Do not change replication “shape” (new replicated fields/conditions) unless you STOP and ask first.

RULES / INVARIANTS (MUST HOLD):
- Server authority for outcomes and placement.
- Deterministic ordering where resolution matters (sort by stable IDs like `PlayerId`, `FBrawlUnitInstanceId.Value`).
- Arena transfer: guest-home occupancy cleared after transfer (avoid duplicates). Return semantics preserve bench shuffles.

INVESTIGATION START (REQUIRED):
1) Use `code_search` to locate the relevant test(s). Search terms to try:
   - "Multi-arena", "MultiArena", "Arena transfer", "Pairings", "ActiveBoardActor", "ArenaResolved"
   - and/or search in `Source/**/Private/Tests/` for "arena".
2) Identify *why* the test fails or flakes:
   - nondeterministic iteration order (TMap/TSet / unsorted arrays)
   - timing assumptions (latent commands too short/too long)
   - missing/late state transitions (ActiveBoardActor restore, guest board cleanup)
   - incorrect tie-break ordering or per-arena resolved gating
3) Fix root cause (prefer runtime correctness; if runtime is correct, harden the test).

DELIVERABLES:
- Code changes that make the test deterministic and passing.
- If you change runtime behavior, include a short rationale referencing the plan milestone.

ACCEPTANCE CRITERIA:
- The multi-arena automation test(s) should be stable (no flakes) and consistent with v3.1 requirements.
- No new tags/attributes/dataasset types.

OUTPUT FORMAT:
- Files modified (paths)
- Brief summary of root cause + fix
- Suggested automation test(s) to run (names)


## PROMPT 2 / Workstream: v3.3 Scouting state (server-authored) + ReplicationGraph routing hook

ROLE: Senior Unreal C++ engineer. You own **server-authoritative scouting selection state** and ensuring ReplicationGraph can route the *scouted* board to the viewer.

CONTEXT:
- Per `rules/brawl-plan.md`, v3.3 spectator/scouting UX is partial.
- RepGraph is implemented (v3.2) and ActiveBoardActor exists; scouting should add a *second board* of interest for non-spectators.

SCOPE (DO):
- Implement/finish a server-authored scouting request flow (client request -> server validates -> server sets scouted selection state).
- Ensure `UBrawlReplicationGraph` (or the per-connection gather logic) can include the viewer’s scouted board as relevant.

SCOPE (DO NOT):
- Do not implement UI widgets; keep UI read-only.
- Do not change spectator economy visibility policy unless explicitly required; if you think policy is ambiguous, STOP and ask.
- Do not add new replicated fields/conditions unless you STOP and ask.

RULES APPLIED (MUST FOLLOW):
- `brawl-replication-contract.md`:
  - clients send requests only; server validates; RepGraph controls relevance
  - non-spectator: relevant boards = active board + scouted board (if any)
- `brawl-player-avatar-camera-contract.md`:
  - scouting is server-authored state; camera state remains client-only

IMPLEMENTATION GUIDELINES:
1) Locate existing scouting stubs first (search "Scout", "Scouted", "RequestScout").
2) Prefer storing scouted selection on PlayerState (server-authored) and owner-only replicated (viewer-only).
3) Add rate limiting + validation:
   - target player exists
   - prevent self-scout no-op (allowed but should behave predictably)
   - sane request frequency
4) Wire RepGraph:
   - per-connection gather should include `ActiveBoardActor` and `ScoutedBoardActor` (or the board derived from `ScoutedPlayerId`).
   - keep determinism: stable ordering when adding nodes.

DELIVERABLES:
- Working scouting request/state pipeline (C++).
- RepGraph uses scouted board in relevancy for non-spectators.

ACCEPTANCE CRITERIA:
- Scouting state is server-authored, validated, and replay-friendly (via replication, not client camera).
- No new tags/attributes.

OUTPUT FORMAT:
- Files modified (paths)
- Summary of replication impact (should be none or very small)
- Suggested tests to add/run (if any)


## PROMPT 3 / Workstream: v3.3 Player avatar teleport + forced Board camera on ActiveBoard/scout changes

ROLE: Senior Unreal C++ engineer. You own **avatar teleport + camera mode switching integration points** (not camera tuning). Implement the TFT-style behavior:
- ActiveBoardActor change => force Board camera + teleport avatar to that board
- Scout target change => force Board camera + teleport avatar to that board

CONTEXT:
- Per `rules/brawl-plan.md`, v3.3 includes "Player avatar + camera integration (GASP + GameplayCameras)".
- Per `brawl-player-avatar-camera-contract.md`, teleport is **server-authored**, camera mode is **client-only**.

SCOPE (DO):
- Ensure server teleports the player’s pawn/avatar when:
  - `ActiveBoardActor` changes
  - scouting selection resolves to a different board
- Ensure client forces Board camera mode on those same triggers.

SCOPE (DO NOT):
- Do not add new gameplay rules to BrawlUI.
- Do not author camera rigs or Blueprint assets unless you STOP and ask for desired asset setup.
- Do not make avatar position gameplay-relevant.

IMPLEMENTATION NOTES:
1) Find existing camera switching hooks in `ABrawlPlayerController` (search "Camera", "Board camera", "ViewTarget", "ActiveBoardActor").
2) Teleport destination must be board-defined:
   - Prefer an existing board API (e.g., `TryGetViewTargetTransformForPlayer`) or add a minimal new board function for a "presence anchor".
   - If adding a new board API, keep it in BrawlGrid and avoid gameplay coupling.
3) Teleport is server-authored:
   - run on server when state changes
   - use safe checks (pawn valid, world not tearing down)
4) Camera mode switching is client-only:
   - call a local function on the owning client (OnRep or delegate-driven) to force Board camera mode

DELIVERABLES:
- ActiveBoard change => server teleport + client Board camera force.
- Scout change => server teleport + client Board camera force.

ACCEPTANCE CRITERIA:
- No gameplay outcomes depend on avatar location.
- No new replication shape unless you STOP and ask.

OUTPUT FORMAT:
- Files modified (paths)
- Summary of behavior + any remaining TODOs that require Blueprint/content
