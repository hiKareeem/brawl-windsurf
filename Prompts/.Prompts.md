I have the following prompt for implementation. What things should be clarified/what questions do you have before starting? Is this within the scope of a conversation, or would it help to break it up? Please STOP and ask after reviewing the relevant code but before if you have questions before we begin implementation, otherwise, please proceed:

I would like to review the prompts in Prompts/2025.12.29.3.md against the git changelist and assess for accuracy and completeness. There were several implicit decisions that were made that may need to be codified in the documentation. If you notice any, please provide paste-in .md snippets and a suggested location for each.

I would like to develop a set of three prompts I can use to delegate to my in-IDE assistants. I'd like them formatted for AI (eg your syntax/style, doesn't need to be human readable) and placed in a new Prompts/2025.12.30.0.md file. DO NOT look at other files in that folder for reference. Please review the docs/repo particularly @brawl-plan.md and git changelist and derive three (potentially parallel but not strictly necessary, though make dependencies clear) workstreams for the prompts, ideally each scoped to 1-2 conversations if possible. If you have any questions/clarifications please STOP and ask BEFORE finalizing the prompts.

Clarifications I need before implementation (STOP)
These affect exact behavior and test expectations:

# Prompt 1
"1) Should “self-scout clear” be allowed for invalid requesters?
   Right now, TargetPlayerId == OwnerPlayerId clears scouting before any other checks.

Your prompt says:

“reject requests when the requesting player is invalid … and do not change scouted state.”
Questions:

If a requester is a spectator or eliminated, and they call ServerRequestScoutPlayer(OwnerPlayerId) to clear, should that also be rejected (meaning they cannot clear their current ScoutedPlayerId/ScoutedBoardActor)?
Or do you want to allow “clear my scouting” even for spectator/eliminated (despite being a “gameplay request”)?
This is important because to enforce “no state change”, I would move requester validation before the self-clear branch.

2) Logging reason precedence for eliminated players
   Because elimination (Life<=0) automatically sets IsSpectator(true), an eliminated requester will usually satisfy both invalid conditions.

Question:

When Life<=0, do you want the rejection reason to log as eliminated (even though they’re also a spectator), or is OwnerIsSpectator sufficient?
(Implementation-wise, this is just the order of checks / reason string.)

3) Should invalid requester checks happen before rate limiting?
   If we reject spectators/eliminated requesters, we can do it:

before rate limit (so they don’t “consume” rate limit window), or
after rate limit (current pattern is rate limit early).
Question:

Do you care whether invalid requester attempts affect NextScoutAllowedTimeSeconds? My inclination is: validate requester first, then rate-limit only “valid requester” traffic.
4) Automation test expectation: “no change” relative to what baseline?
   For the new automation you requested, I plan to:

First establish a baseline where the viewer is scouting someone (ScoutedPlayerId = Target, ScoutedBoardActor = TargetBoard)
Then flip the viewer into:
spectator requester case
eliminated requester case
Then attempt ServerRequestScoutPlayer(AltTargetId)
Assert scouting state stayed on the original target (unchanged)
Question:

Is that the exact scenario you want, or do you specifically want the baseline to be “not scouting anyone” and assert it stays cleared?
Scope / break-up assessment
This is small and fully in-scope for a single conversation:
1 RPC change (a couple of early-outs + logs)
extend existing deterministic LVL_Sandbox test with 2 more steps (or add a small additional test)
No new replication, tags, or UI needed (matches your constraints)."

# Response 1
1. If a spectator or eliminated player requests, they should not/no longer have a home board to "clear" to if I'm understanding correctly. Alternatively, we can make it so that the empty board UNTIL all spectators/players have left, after which it is inaccessible. The functionality I want is: players have a pawn with a camera as well as boards with host/guest viewtargets. Spectators will NOT have a pawn and only snap between the host viewtargets in planning and the active board viewtargets in combat. Implementation is up to you, I *think* we've been referring to the former as "scouting" and the latter as "spectating" but now I'm not sure we've made the distinction clear enough. 2. Definitely marked as Eliminated, please, we'll need that later for match ranking (1-8). 3. I agree with you. 4. I think this depends on 1, so I'll defer to you.

---

# Prompt 2
