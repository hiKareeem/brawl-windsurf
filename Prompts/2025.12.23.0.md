# 2025.12.23.0 — Workstream A (BrawlNet) + Expanded Post A Backlog

This is the working prompt doc for today.

- References-only (file paths / class names / APIs / acceptance criteria).
- No code snippets.

---

# Workstream A — BrawlNet skeleton (ReplicationGraph + ReplaySubsystem)

## Objective
Introduce a minimal `BrawlNet` module skeleton so ReplicationGraph + replay hooks can be implemented cleanly at Milestone 6+, without forcing gameplay module changes now.

## Rules / constraints (do not violate)
- Dedicated server authoritative only.
- Do not block core gameplay iteration on `BrawlNet`.
- Do not introduce dependencies from core gameplay modules back into `BrawlNet` until there is a concrete usage plan.
- Follow the directory/class map for planned BrawlNet types.

## Primary references
- `rules/brawl-directories-2.md`:
	- `Public/Net/BrawlReplicationGraph.h` (`UBrawlReplicationGraph : UReplicationGraph`)
	- `Public/Subsystems/BrawlReplaySubsystem.h` (`UBrawlReplaySubsystem : UGameInstanceSubsystem`)
	- Status note: `BrawlNet` planned but not present; do not reference until created.
- `rules/brawl-replication-contract.md`:
	- ReplicationGraph is required for scaling + spectate
	- Replay requirement is DemoNetDriver based

## Tasks (skeleton-only)
1) Add new module folder structure:
	- `Source/BrawlNet/Public/Net/`
	- `Source/BrawlNet/Public/Subsystems/`
	- `Source/BrawlNet/Private/Net/`
	- `Source/BrawlNet/Private/Subsystems/`
2) Add stub classes (no behavior changes)
	- `UBrawlReplicationGraph`
	- `UBrawlReplaySubsystem`
3) Wire module build + project registration
	- `Source/BrawlNet/BrawlNet.Build.cs`
	- `.uproject` modules list (ensure load order is sane)
4) Confirm no gameplay module takes a hard dependency on BrawlNet yet (unless explicitly decided today).

## Acceptance criteria
- Project builds with `BrawlNet` enabled.
- No runtime behavior changes to Match/Grid/Unit/Economy/Abilities.
- No new cross-module references into `BrawlNet` unless explicitly planned.

## Decision points (STOP and confirm before implementing)
- Do you want `BrawlNet` created today, or deferred until Milestone 6 work begins? Yes
- If created today, should we also add placeholder config entries for ReplicationGraph selection and replay recording toggles, or keep it pure stubs? Yes

---

# Expanded post A–E backlog (prompt-style)

This backlog is intentionally “next-milestones focused” rather than micro-tasks.

## Backlog 1 — Traits v2: thresholds + effects (not just counting)
- Goal: Extend `UBrawlTraitComponent` beyond counts to drive actual gameplay effects.
- References:
	- `Source/BrawlEconomy/Public/Components/BrawlTraitComponent.h`
	- `Source/BrawlEconomy/Public/Data/BrawlTraitData.h`
	- `rules/brawl-implementation-2.md` (“Traits: active vs potential”)
- Constraints:
	- Effects must be server-authoritative.
	- UI remains presentation-only (no gameplay rules).
- Acceptance:
	- Trait thresholds can grant GameplayEffects / abilities in a data-driven way.
	- Active vs Potential is preserved (effects apply only for Active).

## Backlog 2 — Items v1: data + equip/apply pipeline
- Goal: Introduce a first-pass item system that can grant effects/abilities.
- Likely references:
	- `rules/brawl-data-model-contract.md`
	- `rules/brawl-ability-authoring-guide.md`
	- (Module ownership per `rules/brawl-directories.md`)
- Acceptance:
	- Items are authored as DataAssets.
	- Server validates equip/apply requests.

## Backlog 3 — Arena transfer model (8-player future): host/guest halves + return
- Goal: Implement the match-owned orchestration that transfers opponent units to the arena host board during combat, then returns them after.
- References:
	- `Source/BrawlGrid/Public/Actors/BrawlBoardActor.h` (host/guest ids, coord helpers)
	- `Source/BrawlMatch/Public/Components/BrawlRoundManagerComponent.h`
	- `rules/brawl-implementation.md` (arena terminology + transfer mapping)
- Acceptance:
	- Transfer does not break roster ownership.
	- Bench/field legality rules remain phase-gated.

## Backlog 4 — Replay + event log export (Milestone 6)
- Goal: Start/stop DemoNetDriver recording and export structured logs on match end.
- References:
	- `Source/BrawlMatch/Public/Subsystems/BrawlMatchEventLogSubsystem.h`
	- (If/when created) `Source/BrawlNet/Public/Subsystems/BrawlReplaySubsystem.h`
	- `rules/brawl-event-log-schema.md` (timebase + ordering)
- Acceptance:
	- Replays can be recorded server-side and played back.
	- Event log export is stable and schema-versioned.

## Backlog 5 — ReplicationGraph relevancy (Milestone 6+)
- Goal: Scale relevance for 8 boards / 4 arenas + spectator.
- References:
	- `rules/brawl-replication-contract.md`
	- (If/when created) `Source/BrawlNet/Public/Net/BrawlReplicationGraph.h`
- Acceptance:
	- Players see only their relevant boards by default; spectator can opt into all.

## Backlog 6 — UI MVVM wiring (BrawlUI)
- Goal: Bind replicated state (shop offers, board occupancy, trait counts, round timer) to ViewModels.
- References:
	- `rules/brawl-directories-2.md` (planned ViewModels)
	- `Source/BrawlMatch/Public/Game/BrawlGameState.h` (phase/time)
	- `Source/BrawlMatch/Public/Game/BrawlPlayerState.h` (shop/traits/roster)
	- `Source/BrawlGrid/Public/Actors/BrawlBoardActor.h` (occupancy)
- Acceptance:
	- Widgets do not contain gameplay rules.

---

## Notes for today
- If Workstream A is deferred, pick the next backlog item to promote into a scoped workstream with acceptance criteria and tests.
